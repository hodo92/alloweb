import { OutstandingTasksPipe } from './outstanding-tasks.pipe';

describe('OutstandingTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new OutstandingTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
